<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">

        <activity
            android:name="com.wyjson.gesturebuilderdemo.GestureBuilderActivity"
            android:exported="true"
            android:label="@string/application_name">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

        </activity>

        <activity
            android:name="com.wyjson.gesturebuilderdemo.CreateGestureActivity"
            android:label="@string/label_create_gesture" />

        <activity
            android:name="com.wyjson.gesturebuilderdemo.DiscernGestureActivity"
            android:label="@string/label_discern_gesture" />

    </application>

</manifest>